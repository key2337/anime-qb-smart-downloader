# 后续改进方向

## 1. 搜索与匹配改进

### 1.1 标题匹配精度
- **问题**：dmhy 关键词搜索是精确子串匹配，短词容易误匹配（如搜 "Air" 匹配到 "Air Gear"）
- **方向**：
  - 对候选标题做分词/边界判断，减少子串误匹配
  - 利用已解析出的 `parsed_title` 做二级匹配，而非仅依赖原标题
  - 对短查询词（≤3 字符）提高匹配置信度门槛

### 1.2 别名 / 同义词搜索
- **问题**：简称搜不到全称内容（如 "实力至上教室" 搜不到 "实力至上主义的教室"）——实际上 dmhy 标题通常含全称，但简称不一定是全称的子串
- **方向**：
  - 恢复本地别名配置（`title_aliases`），用户可自定义别名映射
  - 或接入外部元数据源（Bangumi/AniList）做标题扩展，但不依赖其做跨语言搜索

### 1.3 季度检测
- **问题**：第一季无标记（`season=None`），中文 "第四季" 未被数字正则匹配（`第\d+季` 不匹配 "第四季"）
- **方向**：
  - 增加中文数字季度检测：第一季～第十二季
  - 从集数范围推断季度（如 ep 50+ 大概率是后续季度）
  - 用户可指定作品总季数，帮助推断

### 1.4 去重优化
- **问题**：同一天多个字幕组发布同一集，候选列表冗长
- **方向**：
  - 按 "标题 + 集数 + 分辨率" 聚合，显示多版本入口
  - 或前端按字幕组分组展示

### 1.5 搜索建议
- **问题**：0 结果时仅提示放宽条件，未建议换关键词
- **方向**：
  - 记录常用搜索词 → 结果的映射，推荐相近关键词
  - 搜索结果不足时自动尝试去掉部分过滤词

### 1.6 按分类 / 标签过滤
- **问题**：dmhy RSS 不返回分类信息（动画/音乐/日剧等混在一起）
- **方向**：
  - 解析 dmhy 页面获取分类标签
  - 或通过标题特征过滤非动画内容（如 ED/Single/OST）

---

## 2. 扩展中文 RSS 源

目前仅接入 dmhy（动漫花园），内容覆盖面有限（老番、冷门番可能没有资源）。

### 2.1 候选源

| 源 | URL | 特点 | 接入难度 |
|---|---|---|---|
| **Mikan Project** | mikanani.me | 中文番剧 RSS，按番剧聚合，带字幕组标签 | 低 — 有标准 RSS |
| **ACG.RIP** | acg.rip | 中文动漫 tracker，RSS 支持关键词 | 低 — 类似 dmhy |
| **Bangumi** | bgm.tv | 番组计划，有 RSS 但更适合元数据 | 中 — RSS 限特定 subject |
| **Nyaa (中文过滤)** | nyaa.si | 国际 tracker，可加中文关键词过滤 | 中 — 标题多为英文 |

### 2.2 接入策略
- 每个源通过统一接口 `fetch_rss(source, keyword)` 接入，`rss.py` 已支持关键词 URL 构建
- 配置文件中新增 `rss_sources` 条目即可，无需改代码
- 多源结果合并去重（按 info_hash 或标题相似度）
- 前端可显示每条结果的来源，方便用户判断

### 2.3 优先级
1. **Mikan Project** — 最活跃的中文番剧 RSS，适合补全 dmhy 缺失的剧集
2. **ACG.RIP** — 补充 tracker 源，增加种子可用性
3. 考虑让用户自定义 RSS 源 URL（如自建 RSSHub 实例）

---

## 3. 其他

### 3.1 下载功能
- 当前搜索工作台只展示结果，未接入下载
- 需实现：选中候选 → 发送 magnet/URL 到 qBittorrent → 监控下载进度
- 已有 `qbittorrent.py` 客户端，需串联 UI

### 3.2 做种数
- dmhy RSS 无做种数字段，所有结果显示 0
- 可从 tracker announce 获取，或接入支持做种数的源（如 Nyaa）

### 3.3 性能
- 每次搜索请求 RSS 并解析 500 条目，约 5-8 秒
- 可考虑：缓存 RSS 结果（30 秒 TTL）、后台预取热门关键词
